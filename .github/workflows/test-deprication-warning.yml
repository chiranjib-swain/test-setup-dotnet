name: test deprication warning

on:
  workflow_dispatch:
  

jobs:
 
 test:
    runs-on: [ubuntu-24.04]
    steps:
    - uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '7.0.x' # Specify the .NET 8.0 SDK version
